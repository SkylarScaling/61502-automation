---
apiVersion: v1
kind: Secret
metadata:
  name: trident-fsx-config
  namespace: "{{ app_namespace }}"
data:
  management-lif: "{{ fsx_management_lif }}"
  data-lif: "{{ fsx_data_lif }}"
  svm: "{{ fsx_svm }}"