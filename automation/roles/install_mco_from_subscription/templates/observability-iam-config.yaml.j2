apiVersion: v1
kind: Secret
metadata:
  name: observability-iam-config
  namespace: "{{ app_namespace }}"
data:
  aws_account_id: "{{ aws_account_id }}"
  aws_s3_role_name: "{{ aws_s3_role_name }}"
type: Opaque